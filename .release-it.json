{
  "github": {
  	"release": true,
    "releaseName": "v${version}",
    "releaseNotes": "npx auto-changelog --stdout --hide-credit --unreleased --sort-commits=date-desc --ignore-commit-pattern=\"Release\\s|Merge\\s\" --template $GITHUB_WORKSPACE/changelog.hbs $last_version $unreleased-only"
  },
  "npm": {
    "publish": false,
    "ignoreVersion": true
  },
  "plugins": {
    "@release-it/bumper": {
      "in": {
        "file": "VERSION"
      },
      "out": {
        "file": "VERSION"
      }
    }
  }
}